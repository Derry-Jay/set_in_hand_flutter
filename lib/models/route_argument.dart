class RouteArgument {
  final int? id;
  final dynamic params;
  final String? tag, content, stuff, word;
  RouteArgument(
      {this.id, this.tag, this.content, this.params, this.stuff, this.word});
}
